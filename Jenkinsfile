pipeline {
   agent {label 'pod'}
         stages{
                  stage('Test docker'){
                           steps{
                                    sh 'docker ps'
                           }
                  }
                  
              
         }
}
